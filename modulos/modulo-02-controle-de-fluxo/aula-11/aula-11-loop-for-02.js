const fruits = ["maçã", "banana", "laranja", "uva"];

for (let i = 0; i < fruits.length; i++) { // length, pega o tamanho do array
    console.log(fruits[i]); // pega a fruta pela indice atual do loop
}