let count = 0;

while (count <= 20) {

    if (count === 8) {
        break; // Interrompe o loop quando count for igual a 8, ignorando o restante do código abaixo e saindo do loop.
    }

    console.log(count);
    count++;
}