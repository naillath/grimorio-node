// Método shift(), responsável por remover o primeiro item de um array

const classes = ["Mago", "Guerreiro", "Arqueiro", "Sacerdote"];

const mago = classes.shift(); // Assim como no método pop(), também consegue tá atribuindo o valor a uma variavel

console.log(mago); // Mago

console.log(classes);
