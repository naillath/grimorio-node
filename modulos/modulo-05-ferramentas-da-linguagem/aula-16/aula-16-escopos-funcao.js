// Escopo de função: Variáveis declaradas dentro de uma função têm escopo local à função. Elas não podem ser acessadas fora da função.

function main() {
    var myName = "Naillath"; // Mesmo usando var, a variável myName tem escopo local à função main. Ela não pode ser acessada fora dessa função.
}

console.log(myName);